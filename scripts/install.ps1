#Requires -Version 5.1
$ErrorActionPreference = 'Stop'
throw 'Native Maestro release binaries currently support macOS and Linux only. Windows support is tracked separately; no Node/Bun fallback is provided.'
