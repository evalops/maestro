# Headless Protocol Design Note

The live contract reference for Maestro's headless JSON-over-stdio interface now
lives at [../protocols/headless.md](../protocols/headless.md).

Use that protocol reference when building or reviewing clients such as Chat,
the Rust TUI transport, or other external embedders.

This design note remains as a pointer so older links do not break.
