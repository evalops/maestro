# Native library consumed by the Platform Bazel service graph.
load("@rules_rust//cargo:defs.bzl", "cargo_toml_env_vars")

load("@rules_rust//rust:defs.bzl", "rust_library")

package(default_visibility = ["//visibility:public"])

cargo_toml_env_vars(
    name = "cargo_toml_env_vars",
    src = "Cargo.toml",
)

rust_library(
    name = "maestro_sandbox",
    deps = [
        "@crates//:dirs-6.0.0",
        "@crates//:dunce-1.0.5",
        "@crates//:libc-0.2.186",
        "@crates//:rand-0.9.4",
        "@crates//:regex-1.13.0",
        "@crates//:serde-1.0.228",
        "@crates//:serde_json-1.0.150",
        "@crates//:thiserror-2.0.18",
        "@crates//:tokio-1.52.3",
        "@crates//:uuid-1.23.4",
    ] + select({
        "@rules_rust//rust/platform:x86_64-unknown-linux-gnu": [
            "@crates//:landlock-0.4.7",  # cfg(target_os = "linux")
            "@crates//:seccompiler-0.5.0",  # cfg(target_os = "linux")
        ],
        "//conditions:default": [],
    }),
    compile_data = glob(
        allow_empty = True,
        include = ["**"],
        exclude = [
            "**/* *",
            ".tmp_git_root/**/*",
            "BUILD",
            "BUILD.bazel",
            "WORKSPACE",
            "WORKSPACE.bazel",
        ],
    ),
    crate_root = "src/lib.rs",
    edition = "2021",
    rustc_env_files = [
        ":cargo_toml_env_vars",
    ],
    srcs = glob(
        allow_empty = True,
        include = ["**/*.rs"],
    ),
    target_compatible_with = select({
        "@rules_rust//rust/platform:x86_64-unknown-linux-gnu": [],
        "//conditions:default": ["@platforms//:incompatible"],
    }),
    version = "0.1.0",
)
