[package]
name = "composer-tui"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Native terminal UI renderer for Composer"

[[bin]]
name = "composer-tui"
path = "src/main.rs"

[lib]
name = "composer_tui"
path = "src/lib.rs"

[dependencies]
# Terminal handling
crossterm = { version = "0.28", features = ["bracketed-paste", "event-stream"] }
ratatui = { version = "0.29", features = ["unstable-widget-ref"] }

# Async runtime
tokio = { version = "1", features = ["rt-multi-thread", "macros", "io-std", "io-util", "sync", "signal", "fs", "process"] }
tokio-stream = "0.1"
tokio-util = "0.7"
futures = "0.3"

# HTTP client for AI APIs
reqwest = { version = "0.12", features = ["json", "stream"] }

# Serialization for IPC
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# SSE streaming for OpenAI Responses API
eventsource-stream = "0.2"
pin-project-lite = "0.2"

# Utilities
unicode-width = "0.2"
textwrap = "0.16"
anyhow = "1"
thiserror = "2"
once_cell = "1"
uuid = { version = "1", features = ["v4"] }
clap = { version = "4", features = ["derive"] }
glob = "0.3"
regex = "1"
dirs = "6"
toml = "0.8"
libc = "0.2"
rayon = "1.10"

# Markdown parsing
pulldown-cmark = "0.12"

# Syntax highlighting for code blocks
syntect = { version = "5", default-features = false, features = ["default-syntaxes", "default-themes", "parsing", "regex-fancy"] }

# Diff generation
similar = "2"

# Random selection for tooltips
rand = "0.9"

# URL parsing for file:// paths
url = "2"

# Shell escaping/unescaping
shlex = "1"

# Base64 encoding for images
base64 = "0.22"

# Terminal color detection
supports-color = "3"

# Date/time handling
chrono = "0.4"

# YAML parsing for skill files
serde_yaml = "0.9"

# Clipboard support (optional)
arboard = { version = "3", optional = true }

# Lua scripting for hooks (optional)
# Uses vendored Lua to avoid system dependency
# "send" feature makes Lua thread-safe (uses Arc instead of Rc)
mlua = { version = "0.10", features = ["lua54", "serialize", "vendored", "send"], optional = true }

# WASM runtime for hook plugins (optional)
wasmtime = { version = "27", optional = true }

# File watching for hot reload
notify = { version = "7", optional = true }

# Linux sandboxing (Landlock + seccomp)
[target.'cfg(target_os = "linux")'.dependencies]
landlock = "0.4"
seccompiler = "0.4"

[features]
default = []
clipboard = ["arboard"]
# Hook backends
lua = ["mlua"]
wasm = ["wasmtime"]
hot-reload = ["notify"]
# Enable all hook features
hooks-full = ["lua", "wasm", "hot-reload"]

[dev-dependencies]
pretty_assertions = "1"
tempfile = "3"
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "hooks_bench"
harness = false

[[bench]]
name = "batch_bench"
harness = false

[[bench]]
name = "indexer_bench"
harness = false

[[bench]]
name = "mcp_bench"
harness = false

[profile.release]
lto = true
codegen-units = 1
strip = true
