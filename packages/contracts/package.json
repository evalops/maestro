{
	"name": "@evalops/contracts",
	"version": "0.10.0",
	"description": "Shared Composer contracts for frontend/backend integration",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./dist/index.js"
		},
		"./package.json": "./package.json"
	},
	"sideEffects": false,
	"files": ["dist"],
	"scripts": {
		"clean": "rm -rf dist ../../tmp/tsbuildinfo/contracts.tsbuildinfo",
		"build": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\" && node -e \"require('fs').mkdirSync('../../tmp/tsbuildinfo', { recursive: true })\" && tsc -p tsconfig.build.json",
		"dev": "node -e \"require('fs').mkdirSync('../../tmp/tsbuildinfo', { recursive: true })\" && tsc -p tsconfig.build.json --watch --preserveWatchOutput"
	},
	"dependencies": {
		"@sinclair/typebox": "^0.34.0"
	},
	"keywords": ["composer", "contracts", "evalops"],
	"author": "EvalOps",
	"license": "MIT"
}
