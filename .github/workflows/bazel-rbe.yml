name: Bazel RBE

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - .bazelignore
      - .bazelrc
      - .bazelversion
      - .github/workflows/bazel-rbe.yml
      - .node-version
      - BUILD.bazel
      - MODULE.bazel
      - MODULE.bazel.lock
      - Makefile
      - docs/development/bazel.md
      - package.json
      - packages/**/package.json
      - scripts/run-bazel-rbe.sh
      - tool-versions.json
      - tools/bazel/**

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  smoke:
    if: github.event_name == 'workflow_dispatch' || vars.BAZEL_RBE_ENABLED == 'true'
    runs-on:
      - self-hosted
      - Linux
      - X64
      - evalops-maestro-internal-rbe
    timeout-minutes: 20
    env:
      BAZEL_RBE_USE_EXISTING_TUNNEL: "true"
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v5.6.0 (tag v5)
        with:
          go-version: "stable"
          cache: false

      - name: Install Bazel tools
        env:
          GOFLAGS: -mod=mod
        run: |
          set -euo pipefail
          go install github.com/bazelbuild/bazelisk@latest
          go install github.com/bazelbuild/buildtools/buildifier@latest

      - name: Verify Bazel graph
        run: make bazel-check

      - name: Run Buildfarm smoke test
        run: make bazel-rbe-smoke
